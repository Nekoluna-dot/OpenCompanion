# New Adds (v1.0.4)

## 主动触发系统提示词优化

重写三类主动触发的 system 提示词，强调"像真人开口"且不暴露机制：

- **事件到期提醒（首次·闹钟响）**：闹钟响为引子，备注 + 完整时间 + 当初的约定原话，直接开口提醒
- **事件追催提醒（第 N 次·闹钟又响）**：第 N 次响铃、已提醒 N-1 次未回，比上次更醒目并带具体细节；第 3 次起支持 `<stop>` 终止后续闹钟
- **主动问候·沉默（revive）**：久无来往自然断开后主动开口，找新话题/问在干嘛/表情包试探；时间改为完整 `YYYY-MM-DD HH:MM`

## Docker 数据持久化加固

- 新增 `MCP/OB/buckets` 挂载：OmbreBrain 记忆桶、向量库、事件账本、脱水缓存落盘，重建容器不再丢数据
- 清理 compose/Dockerfile/gitignore 中的冗余注释
- 端口保持仅暴露 `9000`（网页控制台）与 `18001`（OmbreBrain）

> 注意：数据类目录（`data/`、`conversation/`、`MCP/OB/buckets/`）均不入 git，更新代码后需自行同步或保留 NAS 宿主目录。